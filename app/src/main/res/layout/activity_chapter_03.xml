<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:layout_width="match_parent"
              android:layout_height="match_parent"
              android:orientation="vertical"
              android:padding="@dimen/common_padding">

    <Button
        android:id="@+id/btn_chapter03_common_widgets"
        style="@style/primary_button"
        android:text="Common Widgets"/>

    <Button
        android:id="@+id/btn_chapter03_chat_page_lv"
        style="@style/primary_button"
        android:text="Chat Page -- ListView"/>

    <Button
        android:id="@+id/btn_chapter03_chat_page_rv"
        style="@style/primary_button"
        android:text="Chat Page -- RecyclerView"/>

</LinearLayout>